import ClockConfig from "./Clock/ClockConfig"
import CPUConfig from "./CPU/CPUConfig"
import TodoListConfig from "./TodoList/TodoListConfig"

const widgetList = ['Clock', 'CPU', 'TodoList']

export default widgetList
export {ClockConfig, CPUConfig, TodoListConfig}